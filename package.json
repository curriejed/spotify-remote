{
  "name": "spotify-remote",
  "version": "0.1.0",
  "private": true,
  "description": "A static Spotify remote and podcast queue front end for Vercel.",
  "scripts": {
    "build": "node --check app.js && node --check server.mjs",
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "vercel-build": "npm run build"
  },
  "engines": {
    "node": "22.x"
  }
}
